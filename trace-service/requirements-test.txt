# Test dependencies for trace-service.
# Install with:  pip install -r requirements-test.txt
# (trace-service uses pyproject.toml for app deps.)

pytest>=8.0,<9.0
pytest-asyncio>=0.23
pytest-cov>=5.0
aiosqlite>=0.20.0
faker>=26.0
freezegun>=1.4
